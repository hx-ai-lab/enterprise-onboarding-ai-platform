LLM_API_KEY=
OPENAI_API_KEY=
LLM_BASE_URL=
LLM_MODEL=
PORT=

# Persistent storage for runtime-mutable data (Skills/Tools/Agents/execution
# logs). Leave unset for local dev — falls back to local JSON files under
# mock-data/. In production (e.g. Vercel), set these via the "Upstash for
# Redis" Marketplace integration, or a standalone Upstash Redis database.
# See README.md for details.
KV_REST_API_URL=
KV_REST_API_TOKEN=
